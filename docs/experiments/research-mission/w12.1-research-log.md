# W12.1 Research Mission research log

Status: **active; first measured launch excluded before refreeze**

## Method boundary

This log records preflight, interruptions, excluded evidence, corrections, and
measured execution. The frozen brief, protocol, corpus, and analysis plan remain
the decision boundary. A failed launch is operational and validity evidence; it
is never repaired in place or counted as a product result.

## 2026-09-19 — v1 preflight

The exact executable revision `c896965dbe8f13127d3c5e076df90607274b60ce`
ran one excluded control trial through the `general` LiteLLM route on
`gpuslut01`. It completed in 29.512 seconds, returned the requested model identity
`general`, produced a sealed candidate with only answer, citation, claim, and
opaque identity fields, and stored the private completion with mode 0600.

The two-file excluded preflight tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-preflight-c896965`. Its canonical
relative-path/size/content-digest manifest has SHA-256
`bb49de864de09575e45412e500ec482b24d04ccdb089ff22b207a0b34907b64a`.

## 2026-09-19 — excluded v1 measured launch

The measured downstream launch used revision `c896965dbe8f13127d3c5e076df90607274b60ce`,
model route `general`, concurrency five, and the frozen work order. It was stopped
when two of the first 12 terminal trials failed, exceeding the 10% operational
failure guardrail. Ten trials completed and two failed.

One treatment completion ignored the declared JSON Schema and returned different
claim field names plus no exact obligation result object. It was preserved and
rejected without a formatting retry. A second treatment call returned no string
content. The v1 transport retained successful raw completions but did not retain
the full provider envelope when content was absent, so the second failure cannot
be reconstructed beyond its error record. That is a harness evidence defect as
well as a model-path failure.

No v1 result is eligible for the W12.1 comparison. The 25-file excluded launch
tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-run-c896965`. Its canonical
relative-path/size/content-digest manifest has SHA-256
`a42fbfe3e9edfac621a7697ac5091f97f280650883d7d8ab8ca54197c9cce642`.

## Corrective amendment before v2

The next freeze must:

1. state the exact output field names in the prompt as well as the response
   schema, equally for both arms;
2. retain the exact provider envelope before content extraction or semantic
   validation;
3. pin low reasoning effort and a 10,000-token completion ceiling so hidden
   reasoning is less likely to consume the answer budget;
4. preserve the v1 freeze and excluded evidence unchanged;
5. pass focused control and treatment preflights before a new measured launch.

These corrections change transport observability and common output instructions.
They do not add facts, sources, obligations, budgets, or treatment-only guidance.

## 2026-09-19 — v2 matched preflight

The exact v2 executable revision
`b54aa9b3c766990d88661cf13b69bfacbdf3d97a` ran one excluded control and one
excluded treatment trial through `general` on `gpuslut01`. Both completed with
`finish_reason=stop`, closed response and envelope digests, exact sealed
candidate shape, and private records with mode 0600. The control completed in
13.408 seconds with 511 completion tokens and 10 reasoning tokens. The treatment
completed in 107.083 seconds with 1,366 completion tokens and 148 reasoning
tokens.

The four-file preflight tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-preflight-v2-b54aa9b`. Its
canonical relative-path/size/content-digest manifest has SHA-256
`46cff0b8920a5657477876a9424e8c3639e2b35c2b89483a511151f0b51322e8`.

## 2026-09-19 — excluded v2 measured launch

The v2 measured launch was stopped after three completed downstream trials when
a pre-execution audit found that the pinned analysis program did not emit every
declared sensitivity check. No model failure triggered this stop. Because the
analysis surface was incomplete at launch, the partial tree is excluded in full
and cannot contribute to the W12.1 comparison.

The eight-file launch tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-run-v2-b54aa9b`. Its canonical
relative-path/size/content-digest manifest has SHA-256
`00986e196ed7c2ae9fc4ba5e7e67d0a05741affcd99001f06a7e6ebbdb85dc30`.

## Corrective amendment before v3

V3 adds a resumable independent regrade lane, validates its private evidence and
selection hashes, and emits every sensitivity named by the frozen protocol. The
primary grade remains unchanged. Independent regrades are reported alongside
the original grades so evaluator disagreement is visible. The maximum measured
call ceiling increases from 216 to 288 to cover the worst case in which all 72
downstream candidates require a second grade.

## 2026-09-19 — v3 freeze and preflight reuse

The v3 freeze pins executable revision
`b423ce4c7b50850395c8894a9e2960ec1c46d4f6`. Its downstream mission schema,
prompt builder, and model runner hashes are byte-for-byte identical to v2, so
the passing matched v2 preflight remains applicable. V3 changes only evaluation,
validation, and analysis after downstream candidates exist. No additional paid
preflight call is required before the fresh v3 result tree begins.

## 2026-09-19 — excluded v3 measured launch

The v3 launch was stopped after 23 terminal downstream trials: 22 completed and
one failed. The failure rate was 4.35%, below the declared 10% guardrail. The
failure nevertheless exposed a tooling contradiction: the protocol required
failed-trial missing and worst-case analyses, while the validator and grading
lane still required all 72 downstream calls to succeed. Continuing would have
spent model calls on a result the pinned analyzer could not validly close.

The 47-file partial tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-run-v3-b423ce4`. It contains
1,137,122 bytes. Its canonical relative-path/size/content-digest manifest has
SHA-256 `6d3f3b5765b4387ff3208f2eb252a248da22edc28429cd0621ff4f023a02ac3e`.

## Corrective amendment before v4

V4 permits terminal failures up to the frozen 10% lane guardrail, grades only
completed candidates, and applies the explicit conservative assignment recorded
in the protocol and analysis plan. The measured-call ceiling remains 288; a
failed candidate consumes its primary call but does not create a grading or
independent-regrade call.

## 2026-09-19 — excluded v4 measured launch

All 72 downstream trials completed successfully. The intake lane was stopped
after 14 terminal calls when its third failure raised the lane failure rate to
21.43%. Eleven intake calls completed. Two failures ended with
`finish_reason=length` and no content after using the 10,000-token ceiling. One
returned a valid JSON object followed by extra text, which the strict local
parser correctly rejected.

The 175-file partial tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-run-v4-5a5989d`. It contains
5,631,079 bytes. Its canonical relative-path/size/content-digest manifest has
SHA-256 `c065ce40cee3b4f572066e6a82a795ad00af7e1e45a3262685f12aaf582ad763`.

## Corrective amendment before v5

V5 passes the already-public `IntakeResult` JSON Schema through the provider's
strict structured-output mechanism. The prompt already contained the same
Research Mission schema and intake envelope, so this is a transport enforcement
correction applied equally to all intake cases. No case meaning, reference
answer, grading rule, or downstream comparison changes.

## 2026-09-19 — v5 intake preflight

The exact v5 executable revision
`c7feec3d6a4942810071a83cb25503d43de94c58` ran one excluded intake trial
through `general` on `gpuslut01`. It completed with `finish_reason=stop`, returned
a schema-valid clarification action, closed its response digest, and stored the
private record with mode 0600.

The two-file preflight tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-preflight-v5-c7feec3`. It
contains 49,835 bytes. Its canonical relative-path/size/content-digest manifest
has SHA-256 `17afca430eb1e42ef73c5da7cb033c14e3ab795eb9dffad91e485583fa789a96`.

## 2026-09-19 — excluded v5 measured launch

The downstream lane closed 71 trials and retained one terminal failure, a 1.39%
failure rate inside the guardrail. The corrected intake lane was stopped after
nine terminal calls when two failures raised its failure rate to 22.22%. Both
failed responses used all 10,000 completion tokens as hidden reasoning and
returned no content. Provider-side schema enforcement therefore fixed envelope
shape but not unbounded deliberation on difficult mission construction.

The 165-file partial tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-run-v5-c7feec3`. It contains
4,940,499 bytes. Its canonical relative-path/size/content-digest manifest has
SHA-256 `3fa512b05866492345adffda43b482cb60026882549a8b341449a6a52921537f`.

## Corrective amendment before v6

V6 pins `reasoning_effort=minimal` for intake normalization while leaving every
other model lane at `low`. This preserves the 10,000-token ceiling and the same
semantic task while directly addressing the observed hidden-reasoning
exhaustion. Both v5 failure cases must pass excluded preflights before v6 begins.

## 2026-09-19 — v6 preflight did not close

The exact v6 revision `0689f250bde246e18fd543e2ba3bd19896ea70bb`
completed the excluded control, treatment, and hostile-source intake preflights.
The recovery/portability intake preflight timed out once, then exhausted the
10,000-token ceiling on its second transport attempt. Its receipt reports 8,828
reasoning tokens and no final content. V6 did not enter measurement.

The eight-file preflight tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-preflight-v6-0689f25`. It
contains 448,565 bytes. Its canonical manifest has SHA-256
`de9a3a93dcb82584356b9a646bb25fe684ae5ed831fd48687ced3bf2953af677`.

## Corrective amendment before v7

V7 retains minimal reasoning and raises the intake-only completion ceiling to
20,000 tokens. All downstream and grading calls remain at 10,000. This preserves
the task and schema while allowing the most complex accepted mission enough
room for both deliberation and its required structured result.

## 2026-09-19 — v7 difficult-case preflight

The exact v7 revision `0f9758210245365c089fb0e6a05cc4823d1f4857`
completed the excluded recovery/portability intake case with
`finish_reason=stop`. It used 227 completion tokens, including 78 reasoning
tokens, closed its response digest, and stored the private record with mode
0600. The returned clarification may still be semantically wrong; that is a
measured quality question for the blind intake grader, not a transport failure.

The two-file preflight tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-preflight-v7-0f97582`. It
contains 19,098 bytes. Its canonical manifest has SHA-256
`69e78cfaaedd2e9f29c94fc0733490f7276d3cf1c0fa2f5f96467af8e89b7b0a`.

## 2026-09-19 — excluded v7 measured launch

The downstream lane closed 68 candidates with four failures (5.56%). The intake
lane closed 33 candidates with three failures (8.33%). Both passed their frozen
10% guardrails. Blind downstream grading was stopped after 12 terminal calls
when two failures raised its rate to 16.67%. Both failed grading calls exhausted
10,000 completion tokens as hidden reasoning and returned no grade.

The 242-file partial tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-run-v7-0f97582`. It contains
6,477,857 bytes. Its canonical manifest has SHA-256
`e8c83f4cb24b5ce6bbda9e11ab7df28effaa4ca9ee052a77b4bd3dce79706507`.

## Corrective amendment before v8

V8 applies the proven intake transport bounds to evaluator calls: minimal
reasoning and a 20,000-token completion ceiling. This changes no rubric or
candidate content. Downstream generation keeps its original low/10,000 settings.
The two candidates that exhausted v7 must pass excluded blind-grade preflights.

## 2026-09-19 — v8 grader preflights

The exact v8 revision `a2a743b44bb2c2420d523703ff15afb6e84c97c5`
successfully blind-graded both sealed candidates that exhausted v7. Both ended
with `finish_reason=stop`; one used 9,033 completion tokens and the other 4,903.
The candidate content and rubric were unchanged.

The four-file preflight tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-preflight-v8-a2a743b`. It
contains 236,273 bytes. Its canonical manifest has SHA-256
`aa8eba63322ef6fa8c139cc77ef237f99ee6a64481d288b19937ae1fee7d8159`.

## 2026-09-19 — v8 primary lanes closed; regrade launcher failed

V8 closed all primary lanes within their 10% guardrails: downstream 70 complete
and two failed; intake 36 complete; blind downstream grades 68 complete and two
failed; blind intake grades 35 complete and one failed. The independent regrade
launcher then raised `AttributeError` before writing its work order or making a
model call because it referenced the digest helper from the wrong module.

The 436-file primary tree is retained at
`/home/magnus/.local/state/groktocrawl-x/w12.1-run-v8-a2a743b`. It contains
13,790,342 bytes. Its canonical manifest has SHA-256
`46dadbc8d837bcb863257accbe63dc09d7b2f16c400fd00aa58908311e1f518b`.

## Corrective amendment before v9

V9 uses a local, directly tested canonical digest helper in the regrade launcher.
Because no regrade call occurred and this fix cannot affect prior calls, v9 may
inherit an exact copy of the hashed v8 primary tree. The copy must reproduce the
same 436-file digest before any v9 evaluator call. V9 then appends independent
regrades, validation, and analysis without altering inherited records.

## 2026-09-19 — v9 stopped before evaluation

A pre-execution validation audit found that the validator accepted only one
source revision, while the inherited primary manifests correctly retain v8 and
new regrades must identify the current revision. No v9 model call occurred.
V10 adds an explicit inherited-primary revision to validation; it does not relax
content, receipt, digest, replay, or failure-rate checks.

## 2026-09-19 — excluded v10 independent regrades

V10 selected 20 candidates for independent regrading. The lane was stopped after
17 terminal calls: 14 completed and three failed, a 17.65% failure rate. Two
failed completions wrapped the requested grade in `grade` or `blind_grade`; one
omitted `appropriate_abstention` and the hard-boundary fields. These were clean
model finishes and schema-shape failures, not token exhaustion.

## Corrective amendment before v11

V11 states the exact nine required top-level fields in the regrade prompt and
forbids wrapper objects. It retains the same strict schema, blind rubric,
deterministic selection, evaluator settings, and inherited primary evidence.

## 2026-09-19 — v11 validated result

V11 reproduced the 436-file v8 primary tree digest before evaluation, completed
all 20 deterministic independent regrades without failure, and passed full
evidence validation with zero issues. The final 481-file tree contains
15,488,307 bytes and has canonical manifest SHA-256
`f1c9ddaa992542fa012a5c6da7044fc1ec647267d822de54ab7249fe13c5905e`.

The frozen disposition is reject. The typed treatment passed the difficult-case
gate in one of three repetitions, with coverage changes of -12.5%, +25.0%, and
-12.5%. Intake action accuracy was 40%, mean required-field recall was 2.9/100,
and blind intake grades recorded 11 hard-boundary failures and 94 correction
actions. Operational cost, latency, straightforward-regression, and downstream
hard-boundary gates passed. Independent regrades did not recover a treatment
advantage.
