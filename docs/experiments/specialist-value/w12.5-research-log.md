# W12.5 research log

## 2026-09-19 — freeze

The 12-case corpus, separability labels, handoff schema, resource calculation,
review prompt, hashes, and decision gate were committed before model review. The
experiment does not select LangGraph or any other runtime.

## 2026-09-19 — transport recovery

The first 36 calls could not reach LiteLLM over the direct LAN route. The attempt
was retained as one aggregate transport record. The same frozen run was resumed
through an SSH tunnel to `inference.example.internal`, model alias `general`, at concurrency five.

## 2026-09-19 — bounded response retries

The tunneled run completed 32 reviews and produced four malformed response shapes.
Two bounded retries raised completion to 33 of 36. The three remaining failures
all omitted the required paired preference. Earlier malformed records are retained
under `failures/`; terminal failures remain in `reviews/`.

## Decision

Reject generic specialist fan-out and retain the generalist default. Structural
coverage improved, but blinded usefulness missed the practical-effect threshold
in all repetitions and reviewer reliability failed. Preserve the typed handoff
contract as experimental substrate for a future narrow, real-task comparison.
